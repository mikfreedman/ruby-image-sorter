RSpec.describe Ruby::Image::Sorter do
  it "has a version number" do
    expect(Ruby::Image::Sorter::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
